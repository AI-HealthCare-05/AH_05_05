# docker
DOCKER_USER=ozcodingschool
DOCKER_REPOSITORY=ai-health

# fastapi
APP_VERSION=v1.0.0
SECRET_KEY=oz-codingschool-AI-health260127!@
COOKIE_DOMAIN=localhost

# ai-worker
AI_WORKER_VERSION=v1.0.0

# db
DB_HOST=mysql
DB_PORT=3306
DB_EXPOSE_PORT=3306
DB_USER=ozcoding
DB_PASSWORD=Password1234@
DB_ROOT_PASSWORD=Ozcoding1234@
DB_NAME=ai_health
DB_QUERY_LOG_ENABLED=false

# 로그
# root 로거 레벨. uvicorn 의 --log-level 은 uvicorn* 로거에만 적용되므로
# 앱 코드의 로그 레벨은 이 값으로 정한다. DEBUG/INFO/WARNING/ERROR/CRITICAL
LOG_LEVEL=INFO

# redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# internal api
INTERNAL_API_KEY=replace-with-a-long-random-secret

# web push
VAPID_PRIVATE_KEY=replace-with-vapid-private-key
VAPID_PUBLIC_KEY=replace-with-vapid-public-key
VAPID_SUBJECT=mailto:admin@example.com

# alarm worker
ALARM_POLL_SECONDS=10
ALARM_MAX_RETRY_COUNT=3
ALARM_RETRY_BASE_SECONDS=30
ALARM_PUSH_TTL_SECONDS=300
ALARM_CLICK_URL=/

# 초기 ADMIN 시드 (scripts/seed_admin.py)
# 관리자는 자체 가입이 없어 최초 1명은 스크립트로 넣는다.
# 시드 후에는 비밀번호를 변경하고 아래 값을 지운다.
SUPERADMIN_EMAIL=admin@ozcoding.ai
SUPERADMIN_PASSWORD=ChangeMe123!@

# 메일 발송
# console = 실제로 보내지 않고 로그로만 출력 (로컬 개발 기본값)
# smtp    = 실제 발송. 아래 SMTP_* 를 모두 채워야 한다
#
# ★ 운영에서 console 을 쓰면 메일이 발송되지 않고 임시 비밀번호가 평문으로
#   로그에 남는다. 반드시 smtp 를 쓸 것.
#   (관리자 등록 응답은 emailSent: true 인데 수신자는 메일을 못 받고,
#    대신 운영 로그에 평문 비밀번호가 흐르게 된다.)
EMAIL_BACKEND=smtp

# Gmail 을 쓰려면 SMTP_HOST=smtp.gmail.com, SMTP_PORT=587 로 두고
# SMTP_PASSWORD 에는 계정 비밀번호가 아니라 "앱 비밀번호"를 넣어야 한다.
# (Google 계정 > 보안 > 2단계 인증 > 앱 비밀번호 에서 발급)
#
# SMTP_PASSWORD 는 예시 파일에 값을 두지 않는다. 배포 시 시크릿으로 주입할 것.
# 비운 채로 EMAIL_BACKEND=smtp 로 띄우면 앱이 기동하면서
# "EMAIL_BACKEND=smtp 인데 설정이 비어 있습니다: SMTP_PASSWORD" 로 즉시 죽는다.
# 조용히 console 로 흘러가는 것보다 이쪽이 안전하므로 의도된 동작이다.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=replace-with-smtp-account@example.com
SMTP_PASSWORD=
SMTP_FROM=replace-with-smtp-account@example.com
