# frontend/ 하위만 대상입니다. 저장소 루트 .gitignore 는 건드리지 않았습니다.

node_modules/
dist/
dist-ssr/

# 환경변수 실제 값 (.env.example 은 커밋합니다)
.env
.env.local
.env.*.local

# tsc -b 캐시
*.tsbuildinfo

# Tailwind v4 는 .gitignore 에 없는 파일을 전부 클래스 소스로 스캔합니다.
# dist/ 를 여기서 빼면 이전 빌드 결과를 다시 읽어 CSS 가 부풀어 오릅니다(21kB → 34kB).
