# #369 light-clay UI motion capture handoff

## Outcome

The current feature-369 UI was captured through a real Playwright Chromium session against the existing Vite preview at `http://127.0.0.1:44271` (PID 112293). Four final WebM files are non-empty and independently readable by `ffprobe`.

These recordings are VP8 at 390x844 and the recorder-reported 25 fps. They are browser-capture evidence, not 60 fps or physical-device evidence.

## Playable videos

| Scenario | Duration | Bytes | Windows path |
| --- | ---: | ---: | --- |
| Home medication selection, press, completion | 3.20 s | 145,380 | `C:\dev\AH_05_05\.codex-work\feature-369\frontend\test-results-369-motion-capture-20260910\home-completion-press\home-completion-press.webm` |
| Sheet entry and settle | 2.36 s | 91,623 | `C:\dev\AH_05_05\.codex-work\feature-369\frontend\test-results-369-motion-capture-20260910\sheet-entry\sheet-entry.webm` |
| Chat pending dots and source accordion | 1.44 s | 81,598 | `C:\dev\AH_05_05\.codex-work\feature-369\frontend\test-results-369-motion-capture-20260910\chat-loading-sources\chat-loading-sources.webm` |
| Challenge completion and badge acquisition | 2.84 s | 111,288 | `C:\dev\AH_05_05\.codex-work\feature-369\frontend\test-results-369-motion-capture-20260910\badge-acquisition\badge-acquisition.webm` |

Exact probe data is stored in `frontend/test-results-369-motion-capture-20260910/media-probe.json`. The integrated browser checks and screenshot paths are stored in `frontend/test-results-369-motion-capture-20260910/motion-capture-summary.json`.

## Reduced-motion evidence

The integrated pass created three reduced-motion screenshots plus a dedicated reduced badge screenshot. Computed styles verified:

- sheet animation: `none`
- loading spinner animation: `none`
- all five chat pending-dot animations: `none`
- chat chevron transition: `none`
- home pressed transform: `none`
- badge award animation and transform: `none`

The screenshots are under `frontend/test-results-369-motion-capture-20260910/reduced-motion-checks/` and `frontend/test-results-369-motion-capture-20260910/reduced-badge-award/`.

## 114-image gallery comparison

`design-plans/369-clay-revision/coverage.json` contains 114 entries: 57 before and 57 after, forming 57 complete pairs. The integrated verification reported:

- missing files: 0
- invalid PNG files: 0
- non-390x844 images: 0
- copy mismatches: 0
- DOM-copy hash mismatches: 0
- after-capture page, console, and request failures: 0

The browsable comparison remains at `design-plans/369-clay-revision/gallery.html`.

## Source preservation

The capture helper hashed 13 relevant product and E2E files before and after the integrated pass. `sourceHashStable` is `true`, and an independent comparison found 0 changed hashes. No product UI, wording, or feature logic was edited during this rescue; only capture helpers, generated capture artifacts, and this report were changed.

## Capture helper diagnosis

The zero-byte files under `raw-videos/` are interrupted attempts and were deliberately left in place. A successful Playwright context close produces a finalized, playable WebM.

Three helper-only defects were corrected:

1. The capture build did not include `frontend/tests/harness/ui-motion.html`; it is now a second Vite HTML input.
2. The chat source locator was re-resolved after its accessible name changed on click; `aria-controls` is now read before clicking and the collapsed-panel selector includes `#`.
3. Reduced-motion badge API fixtures are now installed before navigation, so initial requests are intercepted.

The integrated capture completed with exit code 0. The only motion-run console noise was one non-blocking 404 on the home scenario; there were no page errors or request failures, and the home completion checks and final screenshot succeeded.
