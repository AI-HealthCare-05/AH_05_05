# #425 Post-PR-446 Follow-up Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Merge the approved #425 shared-control work with the PR #446 baseline, add the two missing development badge headers, and lock the remaining medication depth/border contracts with rendered regressions.

**Architecture:** Work only in a detached worktree created from `origin/main@4ed8c331`; merge `feature/425@4cfc7275` with an explicit merge commit so the result remains a fast-forward descendant of the existing feature branch, preserving the newer #426 selected-slot/product layout and #428 challenge-navigation contracts at conflicts. Add shared `Header` composition only to the development badge pages, and add fixture-driven browser assertions for badge headers plus already-implemented medication depth/border behavior. Keep all runtime state isolated and leave the actual `feature/425` ref unchanged until independent review and explicit handoff authorization.

**Tech Stack:** Git worktrees, React/TypeScript, Vite, Playwright, TypeScript compiler.

**Spec:** `design-plans/remaining-followup-audit-20260912.md`

## Global Constraints

- Never switch, edit, stage, or run a server from the primary checkout.
- Do not occupy or advance `feature/425`, push, open a PR, install dependencies, or copy dependencies.
- Use the existing dependency tree only, with a worktree-local Vite cache, `watch: null`, fixture-only data, explicit `/api` and `/media` pre-proxy `503`, `workers: 1`, `retries: 0`, and `reuseExistingServer: false`.
- Run browser suites serially, one command at a time, and stop the owned server/browser after validation.
- Use `apply_patch` for authored edits and the commit subject convention `feature/425[신동훈]한글`.

---

### Task 1: Merge approved #425 work with the merged PR #446 baseline

**Files:**
- Modify: `frontend/src/app/styles/index.css`
- Modify: `frontend/src/pages/supplements/SupplementsPage.tsx`
- Modify: `frontend/src/pages/challenges/ChallengeBrowsePage.tsx`
- Modify: `frontend/src/pages/challenges/ChallengeMyPage.tsx`
- Modify: `frontend/src/pages/challenges/OfficialChallengeBrowsePage.tsx`
- Modify: `frontend/src/pages/challenges/OfficialChallengeMyPage.tsx`
- Modify: remaining files from commits `4cc250e` and `4cfc727`

- [x] Confirm detached clean `HEAD=4ed8c3317df7231999b1ccc619d24ac40e648325`, `feature/425=4cfc7275e4e8eef44a79f1320ff34b1afcab497f`, and the primary checkout state without modifying it.
- [x] Merge `feature/425@4cfc7275` with `--no-ff`, stopping on conflicts; this retains both the PR #446 main ancestry and the existing feature-branch ancestry for a later non-force fast-forward handoff.
- [x] Resolve only audited conflicts: retain selected-slot `aria-pressed` depth, apply generic CTA exclusions, preserve the #426 Supplements page while adding semantic Tabs, and keep the #428 label `나의 챌린지` in #425 `NavigationTabs`.
- [x] Verify no unexpected source changes and record the integration checkpoint SHA `0f6ccf309e260a010d3a36dface35ffd7d6d6092`.

### Task 2: Establish the isolated fixture test harness

**Files:**
- Create: `frontend/vite.425post446.config.ts`
- Create: `frontend/playwright.425post446.config.ts`

- [x] Point the worktree to the existing dependency tree without installation or copying.
- [x] Configure localhost port `45525`, worktree-local nested Vite cache, `watch: null`, and hard `503` interception for unmocked `/api` and `/media` requests.
- [x] Configure Playwright with one worker, zero retries, no server reuse, and bounded startup/test timeouts.
- [x] Prove port `45525` is free before starting the fixture server.

### Task 3: Specify missing shared badge headers and existing medication visual contracts

**Files:**
- Modify: `frontend/tests/e2e/challenges-entry-badges.spec.ts`
- Modify: `frontend/tests/e2e/430-medication-followups.spec.ts`
- Test: `frontend/tests/e2e/425-shared-controls.spec.ts`

- [x] Add a failing rendered test for shared `Header` structure, title, 44px back action, and safe fallback navigation on development badge list, empty, detail, and not-found routes at 375/390/1280 widths.
- [x] Run the focused test and capture the expected RED failure caused by the current inline headings.
- [x] Add rendered assertions that the medication memo control retains a non-none elevated shadow and the full prescription article border encloses expanded content.
- [x] Confirm each assertion exercises production composition rather than a duplicated implementation detail, and that removing the intended behavior would fail the test.

### Task 4: Implement the minimal development badge header composition

**Files:**
- Modify: `frontend/src/pages/challenges/ChallengeBadgesPage.tsx`
- Modify: `frontend/src/pages/challenges/ChallengeBadgePage.tsx`

- [x] Add shared `Header` and history-aware fallback navigation to the list/empty page.
- [x] Add shared `Header` and a stable badges-list fallback to normal and not-found detail states.
- [x] Remove only the redundant inline page-title headings; preserve badge content, empty CTA, and official-page behavior.
- [x] Run the focused test to GREEN.

### Task 5: Verify the integrated #425 surface

**Files:**
- Test: `frontend/tests/e2e/425-shared-controls.spec.ts`
- Test: `frontend/tests/e2e/challenges-entry-badges.spec.ts`
- Test: `frontend/tests/e2e/supplement-browse.spec.ts`
- Test: `frontend/tests/e2e/369-clay-controls-followup.spec.ts`
- Test: `frontend/tests/e2e/430-medication-followups.spec.ts`

- [x] Run focused badge-header and medication-render tests serially.
- [x] Run the approved #425 shared-control/Home suites and relevant badge/medication regression suites serially.
- [x] Run TypeScript checking and `git diff --check`.
- [x] Capture decoded 375/390/1280 evidence for tabs/badge headers and 390 evidence for memo depth/prescription border.
- [x] Stop owned runtime processes and record exact commands, pass counts, skips, artifact paths, and limitations.

### Task 6: Commit and request independent review

**Files:**
- Create: `C:/dev/AH_05_05/.codex-work/notion-20260912/425-post446-followup-report.md` outside the tracked worktree
- Modify: only the scoped source/tests/docs inside the detached worktree

- [x] Commit only reviewed source, tests, and any durable tracked documentation with a `feature/425[신동훈]...` subject.
- [x] Verify the exact final SHA, clean tracked status, changed-file list, parent/main diff, and that `feature/425` still points to `4cfc7275`.
- [x] Report test evidence and screenshots to the parent and independent reviewer.
- [ ] Do not advance the actual feature ref until independent approval and a fresh explicit authorization from the parent.

### Task 7: Run the final cross-branch shared-tabs smoke after individual handoffs

**Files:**
- Test only in a separate detached integration checkout; do not merge sibling feature heads into one another

- [ ] After the individually approved #425, #429, and #432 heads are available, create a separate detached combined-validation checkout from the current merged main.
- [ ] Merge the three exact approved heads only into that disposable integration checkout, preserving #425 shared Tabs, #429 medication-note tabs, and #432 unrelated notification behavior.
- [ ] At 375/390/1280px, smoke the #429 notes tabs together with #425 Home, Supplements, and challenge tabs, including selected/keyboard semantics and green CTA distinction.
- [ ] Capture user-review images only after returning focus to a neutral heading/background and waiting for transient toasts to be hidden; prefer production fixture routes where available instead of presenting the legacy dev challenge browse composition as the production result.
- [ ] Record results separately; never add sibling merges to any actual feature branch.
