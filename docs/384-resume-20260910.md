# #384 잔여 2건 재개 결과

작성일: 2026-09-10

작업 위치: `/mnt/c/dev/ah_05_05/.codex-work/feature-384`

브랜치/기준: `feature/384` / `451bd86`
상태: 구현 및 집중 회귀 검증 완료, 미커밋 보존

## 완료한 범위

### 1. 비로그인 홈 공개 영양제 랭킹

- 비로그인 홈은 `VITE_USE_MOCK` 설정과 관계없이 공개 API `GET /api/v1/display/med/nutr/rank`를 조회한다.
- 응답의 백오피스 제목, 제품 ID, 제품명, 순위를 그대로 화면 타입으로 매핑한다.
- 현재 공개 전시가 없어 API가 404를 반환하거나 응답 `items`가 비어 있으면 예시/목업 랭킹을 표시하지 않는다.
- 정상적인 데이터 없음과 로딩·오류를 구분하고, 정상 빈 상태에는 `현재 공개된 영양제 랭킹이 없어요.`를 표시한다.
- 로그인 홈과 영양제 둘러보기의 기존 목업/실 API 전환 동작은 유지했다.

관련 제품 파일:

- `frontend/src/entities/supplement/api.ts`
- `frontend/src/entities/supplement/index.ts`
- `frontend/src/pages/home/HomePage.tsx`

### 2. 펼친 처방 안의 전체 약 표시

- 에피소드 상세를 펼치면 해당 처방의 약을 처음부터 모두 표시한다.
- 내부 `약 n개 더보기`와 `약 목록 접기` 버튼 및 전용 상태를 제거했다.
- 에피소드 자체의 펼치기/접기와 3개 이상 처방의 외부 `펼치기`/`접기`는 유지했다.
- 기존 복약 선택, 완료, 되돌리기, 실패 복원 동작은 변경하지 않았다.

관련 제품 파일:

- `frontend/src/pages/home/MedicationTimeline.tsx`

## 테스트

Node `v22.23.1`, WSL에서 실행했다.

- 신규 비로그인 공개 랭킹: 3/3 통과
  - 공개 API 응답만 표시
  - 404 빈 상태
  - 빈 `items` 빈 상태
  - 결과: `frontend/test-results-384-resume-public-final3/.last-run.json`
- 복약 압축 전체 회귀: 12/12 통과
  - 내부 약 목록 토글 제거와 전체 약 표시
  - 외부 에피소드 접기 유지
  - 기존 선택/완료/실패/접힘 처방 동작 포함
  - 결과: `frontend/test-results-384-resume-medication-final2/.last-run.json`
- 기존 랭킹 API·홈 실계약 fixture: 실행 대상 14/14 통과, 목업 전용 4건 스킵
  - 결과: `frontend/test-results-384-resume-ranking-final2/.last-run.json`
- 기존 홈 Figma·#384 완료 요약 회귀: 18/18 통과
  - 결과: `frontend/test-results-384-resume-home-regression/.last-run.json`
- 최종 직렬 verification gate: 목업 모드 21/21 통과, 실계약 모드 실행 대상 26/26 통과·목업 전용 4건 스킵
  - 결과: `frontend/test-results-384-resume-verification-mock/.last-run.json`, `frontend/test-results-384-resume-verification-real/.last-run.json`
- TypeScript `tsc --noEmit`: 통과
- production build: 통과
  - 기존 500 kB 초과 번들 경고 유지
- `git diff --check`: 통과

RED 단계에서는 공개 API 고유 제목 미노출, 404 빈 상태 미노출, 5개 약 중 3개만 표시되는 실패를 각각 확인한 뒤 구현했다. 병렬 검증 중 공유 `node_modules/.vite` 최적화 캐시 경합으로 일부 cold-start timeout이 발생했으나, 모든 최종 결과는 서버를 한 개씩 직렬 실행해 다시 통과시켰다.

## 전후 시각 증거

기준 커밋 `451bd86`을 임시 디렉터리에서 실행하고 기능 워크트리와 동일한 API fixture, 시각, 375×812 viewport로 캡처했다.

- 공개 랭킹 있음: `design-plans/384/screenshots/{before,after}/issue5-public-ranking.png`
- 공개 랭킹 없음: `design-plans/384/screenshots/{before,after}/issue5-ranking-empty.png`
- 펼친 처방 전체 약: `design-plans/384/screenshots/{before,after}/issue6-expanded-episode-all-medications.png`
- 갤러리 설명: `design-plans/384/README.md`

## 범위 및 남은 우려

- 기본 작업 폴더의 `feature/315`와 `feature/369`는 수정하거나 브랜치를 전환하지 않았다.
- backend, DB, worker를 시작·재시작·변경하지 않았다. 공개 API 동작은 기존 backend 테스트와 프론트 API fixture 계약을 근거로 검증했으며, 사용자 로컬 DB의 실제 백오피스 데이터 통합 실행으로 확대해 표현하지 않는다.
- 기존 #333 baseline 실패 2건은 이번 범위에서 수정하거나 재분류하지 않았다.
- commit, push, merge는 수행하지 않았다.
