# #577 홈 복약 시간대 버튼 색상

- 기준: main `6f0f5fd`, 독립 브랜치 `feature/577`.
- 원인: 먹었어요 버튼의 primary 조건에 현재 시간대 여부가 포함되어 과거·다음 시간대는 secondary로 표시됐다.
- 수정: 실행 가능한 미완료 버튼은 모든 시간대에서 기존 primary 색상을 사용한다. 완료·선택·비활성·기록 저장 조건은 바꾸지 않는다.
- 신규 회귀: 저녁 시각으로 시작해 아침·점심·저녁·자기전 버튼의 실제 배경색 일치 및 저장 요청 없음 확인. 수정 전 아침 버튼 secondary로 실패함을 확인했다.
- 테스트 유지보수: 테스트 도중 14시간을 이동해 세션이 만료되는 대신 처음부터 20시로 고정했다. 기존 대기 중 저장 테스트는 현재 main의 처방별 완료 배지를 대상으로 확인하도록 갱신했다.
- 기존 네이티브 터치 검증의 화면 밖 시작 좌표를 화면 내로 보정했다. 제품 스와이프 동작은 변경하지 않았다.
- 검증(2026-09-18): TypeScript 통과, 시간대 전환·완료·되돌리기·네이티브 터치 등 Chromium 11/11 통과. 전체 CI/실기기 검증은 아니다.
- 캡처: `artifacts/577-home-actions/final/`의 `home-아침.png`, `home-점심.png`, `home-저녁.png`, `home-자기전.png`. 실제 앱 + 로컬 합성 API 데이터이며 운영 계정이나 배포 화면이 아니다.
