# OCR 함량 표시 중복 방지 — 2026-09-07

## 요청과 결과

최종 요청인 “중복 방지 위주”를 기준으로 제품명에서 함량을 일괄 삭제하지 않았다.

- 제목: `글로덱시정300mg 300mg 0.3g` → `글로덱시정300mg`.
- OCR 검토 요약: `함량 300mg 0.3g` → `함량 300mg`.
- 원본 제품명과 편집창의 OCR 함량 `300mg 0.3g`은 보존한다. 저장 API/DB 값은 변경하지 않는다.
- 다른 함량 및 복합 비율·농도는 함부로 같은 값으로 합치지 않는다.

## 변경 파일

- `frontend/src/shared/lib/medicationLabel.ts`: 표시 전용 공통 중복 제거. g/mg/μg의 정확한 십진수 비교, 한글·호환·전각 단위 처리. 제품명은 원문을 유지하고 추가되는 동일 함량만 생략한다.
- `frontend/src/pages/ocr-review/OcrReviewPage.tsx`: 하단 함량 요약도 같은 표시 전용 정리 함수를 사용한다.
- `frontend/tests/e2e/ocr-strength-display.spec.ts`: 동일/다른 함량, 단위 환산, 복합 비율, 농도, 호환 문자, 모바일·데스크톱 표시와 편집 원문 보존 검증.

홈과 복약 시간 화면은 기존 공통 함수를 통해 개선된다. 그 외 이름·용량을 직접 조합하는 화면 전체를 이번에 일괄 변경하지는 않았다.

## 검증

- 중복 재현 테스트 RED 확인 후 수정하여 GREEN 확인.
- 호환·전각 문자 회귀도 RED → GREEN 확인.
- `VITE_USE_MOCK=false`, Playwright: `ocr-strength-display.spec.ts` + `home-medication-compression.spec.ts`, **13 passed**.
- 375px / 1280px OCR 검토 화면 스크린샷 직접 확인. 제목 및 요약의 중복이 없고 잘림 없이 표시됨.
- 편집 대화상자의 원본 약품명과 함량 값 보존을 브라우저 assertion으로 확인.
- `tsc -b` 및 `vite build` 성공. 500kB 초과 번들 경고는 남아 있음.
- 관련 파일 `git diff --check` 통과. Git의 LF/CRLF 안내만 출력됨.

확장 검증인 `medication-registration-flow.spec.ts`는 수정 후 **26 passed / 4 failed**다. 수정 전 두 제품 소스 파일을 복원한 별도 진단 복사본에서도 전체 30개를 같은 순서로 실행해 **동일한 26 passed / 4 failed**를 확인했다. 다음 기존 실패는 이번 표시 변경 범위 밖으로 유지했다.

1. 918행: 날짜 수정 저장 후 `/login`으로 이동하는 인증 흐름 실패. 부분 실행에서는 통과하나 전체 실행에서 수정 전후 동일 재현.
2. 1204행: 홈의 `tabpanel` 접근성 role 기대값 불일치.
3. 1343행: 처방 저장 순서 기대값 `[12,24]`와 실제 `[24,12]` 불일치.
4. 1430행: 다중 처방 저장 실패 시 첫 처방 완료 표시 기대값 불일치.

실제 OCR API를 새로 호출하거나 환자 데이터를 변경한 검증이 아니라, 기존 API 형태의 고정 응답을 사용하는 브라우저 표시 검증이다.

## 품질 점검과 범위

`ce-simplify-code`의 재사용·품질·효율 읽기 전용 검토를 반영했다. NFKC 정규화를 토큰화보다 먼저 수행해 기존 호환 문자 처리를 보존하고, 숫자열 중간에서 정규식 탐색을 재시작하지 않도록 경계를 추가했다. 최종 재검토에서 새 blocker는 발견되지 않았다.

Code review: skipped (ce-code-review unavailable). 해당 리뷰의 Git branch/PR 기준으로는 이미 존재한 다수의 미커밋 변경과 이번 변경만을 안전하게 분리할 수 없어, 작업 시작 스냅샷과의 정확한 diff 및 별도 읽기 전용 검토로 대체했다.

다른 함량이나 복합 농도의 동등성을 폭넓게 추론하는 기능은 추가하지 않았다. LLM·백엔드 로직, DB, worker는 이번에 변경하지 않았다. 기존 미커밋 변경을 보존했고 브랜치 생성, 스테이징, 커밋, push는 하지 않았다.

